{
  "name": "lumiere-de-noel-site",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "next dev",
    "build": "node --no-warnings scripts/check-photos.mjs && next build",
    "start": "next start",
    "check:photos": "node --no-warnings scripts/check-photos.mjs"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "next": "^14.2.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0"
  },
  "devDependencies": {
    "vercel": "^51.6.1"
  }
}
